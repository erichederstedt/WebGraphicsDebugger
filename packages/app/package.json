{
  "name": "@wgd/app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:inject": "vite build --config vite.inject.config.ts",
    "preview": "vite preview",
    "preview:inject": "vite preview --config vite.inject.config.ts --host --port 4173"
  },
  "dependencies": {
    "@wgd/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
