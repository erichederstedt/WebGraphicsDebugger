{
  "name": "@wgd/app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:inject": "vite build --config vite.inject.config.ts",
    "preview": "vite preview",
    "preview:inject": "vite preview --config vite.inject.config.ts --host --port 4173",
    "dev:inject": "npm run build:inject && npm run preview:inject"
  },
  "dependencies": {
    "@wgd/core": "*",
    "@zhobo63/imgui-ts": "^0.1.53"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
